# Primeiros passos

```bash
npm i
```

O código das lambdas fica em `rest-api/lambda`

# Deploy

Criando:

```bash
npx cdk deploy --app 'ts-node .'
```

Destruindo:

```bash
npx cdk destroy --app 'ts-node .'
```
